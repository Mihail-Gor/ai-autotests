name: Regression Suite & Allure Reporting

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      tags:
        description: 'JUnit 5 Tag expression (e.g. regression, db, products, users)'
        required: false
        default: 'regression'
        type: string

concurrency:
  group: regression-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  run-regression:
    name: Run Full Regression Suite
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 21 (Temurin)
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Install Playwright Browsers
        run: |
          mvn test-compile -B
          mvn exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps chromium" || true

      - name: Execute Full Test Suite (API + DB + UI)
        run: |
          TAGS="${{ github.event.inputs.tags || 'regression' }}"
          echo "Running tests with tag expression: $TAGS"
          mvn clean test -B -Dgroups="$TAGS"

      - name: Load Allure Report History from gh-pages
        if: always()
        run: |
          if git ls-remote --heads origin gh-pages | grep -q 'refs/heads/gh-pages'; then
            echo "Branch gh-pages found, fetching history..."
            git clone --single-branch --branch gh-pages --depth 1 "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" gh-pages-branch || true
          else
            echo "Branch gh-pages does not exist yet. Skipping history load."
          fi

      - name: Setup Allure History
        if: always()
        run: |
          mkdir -p target/allure-results
          if [ -d "gh-pages-branch/history" ]; then
            echo "Copying previous Allure history to target/allure-results/history..."
            cp -r gh-pages-branch/history target/allure-results/
          fi

      - name: Generate Allure Report
        if: always()
        run: mvn allure:report -B

      - name: Publish Test Summary
        if: always()
        run: |
          echo "### 📊 Regression Run Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Trigger:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Tags:** \`${{ github.event.inputs.tags || 'regression' }}\`" >> $GITHUB_STEP_SUMMARY

      - name: Deploy Allure Report to GitHub Pages
        if: always() && (github.ref_name == 'main' || github.ref_name == 'master')
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: target/site/allure-maven-plugin
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          keep_files: false

      - name: Archive Allure Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: target/allure-results/
          retention-days: 14

      - name: Archive Allure HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-html-report
          path: target/site/allure-maven-plugin/
          retention-days: 14
